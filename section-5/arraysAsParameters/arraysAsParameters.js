// Challenge:
// 1. Create a function, getFirst(arr), that returns the first item in the array
// 2. Call it with an array as an argument to verify that it works


// Solution:
function getFirst(arr) {
    return arr[0];
}

let array = getFirst([1, 2, 3]);

console.log(array);
