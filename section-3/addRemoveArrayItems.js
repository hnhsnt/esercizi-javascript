let cards = [7, 4];

cards.push(6); // this adds 6 to the array like this: [7, 4, 6]
cards.pop(); // this removes the last item on the array: [7]
cards.shift(5); // this adds 5 to the array like this: [5, 7, 4]
cards.unshift(); // this removes the first item on the array.